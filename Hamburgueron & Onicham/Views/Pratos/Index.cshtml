@model Hamburgueria.DATA.DTO.PratosDTO

@{
    ViewData["Title"] = "Cadastrar Prato";
}

<h1>Cadastrar Prato</h1>

<h4>Pratos</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="CadastroPrato">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ValorPrato" class="control-label"></label>
                <input asp-for="ValorPrato" class="form-control" />
                <span asp-validation-for="ValorPrato" class="text-danger"></span>
            <div class="form-group">
                <label asp-for="NomePrato" class="control-label"></label>
                <input asp-for="NomePrato" class="form-control" />
                <span asp-validation-for="NomePrato" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <<a class="btn btn-primary" asp-action="ListarPratos">Voltar para a lista.</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
